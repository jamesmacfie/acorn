{
  "name": "@acorn/desktop-tauri",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm run stage && ACORN_DEV_SERVER=http://localhost:4319 tauri dev --config src-tauri/tauri.dev.json",
    "build": "pnpm run stage && pnpm run build:renderer && pnpm run check:renderer-budget && pnpm run check:runtime-syntax && CI=true tauri build --config '{\"build\":{\"beforeBuildCommand\":\"\"}}' && pnpm run verify:bundle",
    "stage": "pnpm run build:service && pnpm run build:bundled-plugins && pnpm run build:helper && pnpm run build:bridge && node scripts/stage.mjs",
    "build:service": "pnpm --filter @acorn/node build",
    "build:bundled-plugins": "node ../desktop/scripts/build-bundled-plugins.mjs dist/bundled-plugins",
    "build:helper": "vite build --config vite.helper.config.ts",
    "lint": "tsc --noEmit",
    "test": "pnpm run stage && vitest run && cargo test --manifest-path src-tauri/Cargo.toml",
    "tauri": "tauri",
    "build:bridge": "vite build --config vite.bridge.config.ts",
    "build:renderer": "vite build",
    "check:renderer-budget": "node ../desktop/scripts/check-renderer-budget.mjs dist/client",
    "check:runtime-syntax": "node ../desktop/scripts/check-runtime-syntax.mjs dist/bridge dist/helper",
    "verify:bundle": "node scripts/verify-bundle.mjs"
  },
  "//build": "The renderer is built here rather than by `beforeBuildCommand` so the startup-budget check runs against the bytes that get bundled and the bundler pass does not repeat a build that already happened; the inline config empties the hook for that one invocation. CI=true is what makes the DMG's Finder-cosmetics AppleScript get skipped — it needs Automation permission a build machine does not have, so the runner skips it either way and this keeps a local DMG the same shape as the shipped one.",
  "//dependencies": "The node's runtime dependencies. The staged service.js and the helper both resolve their externals from this package's node_modules, so this mirrors apps/desktop for the same reason it exists there.",
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "^0.54.1",
    "@agentclientprotocol/sdk": "^1.3.0",
    "@anthropic-ai/sdk": "^0.113.0",
    "@hono/node-server": "^2.0.11",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@solidjs/router": "^0.16.1",
    "@tanstack/query-async-storage-persister": "^5.101.0",
    "@tanstack/solid-query": "^5.101.0",
    "@tanstack/solid-query-persist-client": "^5.101.0",
    "@tanstack/solid-virtual": "^3.13.29",
    "@tauri-apps/api": "^2.9.0",
    "@vscode/ripgrep": "^1.15.9",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-serialize": "0.13.0",
    "@xterm/addon-webgl": "^0.18.0",
    "@xterm/headless": "5.5.0",
    "@xterm/xterm": "^5.5.0",
    "diff": "^9.0.0",
    "drizzle-orm": "catalog:",
    "gitdiff-parser": "^0.3.1",
    "hono": "catalog:",
    "idb-keyval": "^6.2.5",
    "jose": "^6.2.3",
    "lucide-static": "^1.27.0",
    "monaco-editor": "^0.55.1",
    "node-pty": "^1.0.0",
    "openai": "^6.48.0",
    "pg": "^8.22.0",
    "playwright-core": "^1.61.1",
    "shiki": "^4.2.0",
    "smol-toml": "^1.7.0",
    "solid-js": "catalog:",
    "ws": "^8.21.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@acorn/client-core": "workspace:*",
    "@acorn/desktop-helper": "workspace:*",
    "@acorn/node": "workspace:*",
    "@acorn/protocol": "workspace:*",
    "@tauri-apps/cli": "^2.9.0",
    "@types/node": "^26.0.0",
    "@types/ws": "^8.5.13",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vite-plugin-solid": "^2.11.12",
    "vitest": "^4.1.9"
  }
}
