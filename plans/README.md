# Implementation Plans

Generated by the improve skill on 2026-07-24. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update the row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Add local Claude and Codex usage details to the Agents surface | P1 | L | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale — finding fixed independently or approach
abandoned)

## Dependency notes

- Plan 001 is self-contained and has no prerequisite plan.

## Findings considered and rejected

- Extending the core delegated rail tooltip with structured colored rows:
  rejected for this first version because the existing plain `data-tip-sub`
  contract can render the requested health icons and percentages without
  increasing the core UI blast radius.
- Storing provider usage in SQLite or IndexedDB: rejected because this is an
  ephemeral account-level read model; an in-memory TTL plus last-good snapshot
  is enough and avoids a schema/migration lifecycle.
- Direct Claude/Codex usage APIs: rejected because they make Acorn responsible
  for OAuth credential reads, refreshes, and writes. The installed CLIs and
  Claude's local logs provide the requested first version without new
  credentials.
- Putting usage collectors in the Claude/Codex profile plugins: rejected
  because usage is a shared Agents-surface concern and the requested ownership
  is the Agents plugin. The collectors can identify the two commands without
  expanding the agent-profile contract.
