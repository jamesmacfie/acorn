# Conventions: moved

Part of [docs/future/structure/](./README.md). This was the draft of the naming rules. Phase 1 moved
it to [docs/conventions.md](../../conventions.md), which is now the owning doc: it states each rule in
the present tense and, where the tree does not follow it yet, names the phase that closes the gap.

Read it there. Phase 6 adds a line to each rule saying whether a test enforces it.
