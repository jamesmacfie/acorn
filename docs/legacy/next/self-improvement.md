# Self-improvement follow-ups

The current product supports notes, memory proposals, agent event history, context sections, and
workflow attention. It does not silently rewrite accepted memory, infer hidden context, or let agent
text satisfy workflow control flow.

Any future self-improvement feature must remain annotation/proposal-first, human reviewable, task
scoped, auditable, and bounded by the existing agent-tool and config-trust policies.
