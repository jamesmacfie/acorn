{
  "name": "@acorn/plugin-context",
  "version": "0.1.0",
  "description": "What an agent is told: the task-context snapshot the composer assembles and keys.",
  "private": true,
  "type": "module",
  "exports": {
    "./client/index.ts": "./src/client/index.ts",
    "./contract/*": "./src/contract/*",
    "./testkit/client": "./src/testkit/client.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@acorn/plugin-api": "workspace:*",
    "@acorn/protocol": "workspace:*",
    "solid-js": "catalog:"
  },
  "devDependencies": {
    "@acorn/client-core": "workspace:*",
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vite-plugin-solid": "^2.11.12",
    "vitest": "^4.1.9"
  }
}
