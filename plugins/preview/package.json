{
  "name": "@acorn/plugin-preview",
  "version": "0.1.0",
  "description": "A live preview of what a task is building, plus the page rules the shell enforces around it.",
  "private": true,
  "type": "module",
  "exports": {
    "./node/index.ts": "./src/node/index.ts",
    "./client/index.ts": "./src/client/index.ts",
    "./contract/*": "./src/contract/*"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@acorn/plugin-api": "workspace:*",
    "@acorn/protocol": "workspace:*",
    "solid-js": "catalog:"
  },
  "devDependencies": {
    "@acorn/client-core": "workspace:*",
    "@acorn/node-core": "workspace:*",
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vite-plugin-solid": "^2.11.12",
    "vitest": "^4.1.9"
  }
}
