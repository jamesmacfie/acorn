// The three built-in agent profiles (docs/managed-agents.md § Providers: why they are first-party, and why
// the old profiles-{claude,codex,aider} packages were folded in here).
export { aiderProfile } from './aider'
export { claudeCodeProfile } from './claudeCode'
export { codexProfile } from './codex'
