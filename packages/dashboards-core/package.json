{
  "name": "@acorn/dashboards-core",
  "version": "0.1.0",
  "description": "Pure dashboard shaping both runtimes read: layout, series, trends, and formatting, with no I/O.",
  "private": true,
  "type": "module",
  "exports": {
    "./*": "./src/*"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@acorn/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
