{
  "name": "acorn",
  "private": true,
  "packageManager": "pnpm@11.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build-deploy": "pnpm build && pnpm --filter @acorn/web exec wrangler deploy"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
